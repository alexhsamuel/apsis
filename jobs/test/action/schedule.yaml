params: [label]

program:
  type: no-op

action:
  type: schedule
  job_id: test/action/after
  condition:
    states: success

action:
  type: schedule
  job_id: test/action/fail
  condition:
    states: [failure, error]

