params: [label, bar]

program: 'echo "dep2 label={{ label }} bar={{ bar }}"'

condition:
- type: dependency
  job_id: dep1
  args:
    foo: -42
- type: dependency
  job_id: dep0
  args: {label: coconut}


