params: [date, color]

program:
  type: no-op
  duration: 1

condition:
  - type: dependency
    job_id: dependency
    args:
      flavor: vanilla

  - type: dependency
    job_id: dependency
    args:
      flavor: chocolate

