database:
  path: apsis.db
  timeout: 0.1m
jobs: jobs

procstar:
  agent:
    enable: true
    connection:
      start_timeout: 1 min
      reconnect_timeout: 15
    run:
      update_interval: 10
      output_interval: 5

waiting:
  max_time: 30

